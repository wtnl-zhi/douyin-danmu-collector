# 直播弹幕采集器 0.1.3（macOS ARM64）

## 直接使用

1. 打开 `直播弹幕采集器.app`，输入形如 `https://live.douyin.com/123456` 的直播间链接。
2. 点击“开始抓取”。默认开启“静默抓取（后台 Chrome）”：应用会启动仅监听 `127.0.0.1:9223` 的无界面 Chrome，不会弹出浏览器窗口。
3. 在下方实时流查看用户名、ID、内容与时间；点击“停止抓取”后可导出 CSV、XLSX、SQLite 或蓝色动态 HTML 关键词云。

关键词云已改用 Canvas 3D 实时投影：词语到球背面时按景深变小/变暗，不会出现 CSS 翻转或镜像文字；主词按频率突出，次级词自然退场。导出的 HTML 使用相同引擎，断网也能打开和拖动旋转。

若需要处理登录或人机验证，可在“设置”中关闭静默抓取，临时使用可见的隔离 Chrome；处理完成后再重新打开静默模式。

## 数据与隐私

- 采集链路使用 Chrome DevTools Protocol 中的 `Network.webSocketFrameReceived`，直接读取 WebSocket 二进制帧并解析 Protobuf/Gzip；不读取页面右侧可访问文本，也不做截图/OCR。
- 默认保留原始 WebSocket 帧，以便协议变动时排查；可在设置中关闭。
- 本地 SQLite 路径：`~/Library/Application Support/douyin-danmu-desktop/data/danmu.sqlite`。
- 主要字段：`sent_at_ms`（弹幕时间）、`username`、`user_id`、`content`、`message_id`、`session_id`。`capture_sessions` 保存任务状态，`raw_ws_frames` 保存去重后的原始帧。

请只采集有权访问的直播间，并按平台规则、用户隐私政策和适用法律处理、保存及导出数据。

## 安装包

- macOS ARM64（Apple Silicon）DMG：`douyin-danmu-collector-0.1.3-arm64.dmg`
- SHA-256：`6585881d70908ff6e31e0fe85c5b4ee16f39c79a009b2e744cf556ab3df07c17`

这是本地自签名/未公证的测试交付包，首次运行若被 macOS 拦截，可在 Finder 中按住 Control 点击应用并选择“打开”。

## 已验证

- TypeScript 检查和生产构建均已通过。
- 实测从直播 WebSocket 帧解出并入库 380 条弹幕；静默模式实测捕获 144 条且没有出现 Chrome 窗口。
- 生成的 DMG 已完成校验、挂载检查，内含可启动的 `.app`。
